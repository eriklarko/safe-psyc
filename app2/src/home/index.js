// @flow

import React from 'react';
import { View, Text } from 'react-native';

type Props = {};
export function Home(props: Props) {
    return <View style={{
        marginTop: 30,
    }}>
        <Text>HELLO</Text>
    </View>
}